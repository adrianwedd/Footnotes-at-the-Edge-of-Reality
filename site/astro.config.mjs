// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  base: '/Footnotes-at-the-Edge-of-Reality/',
  site: 'https://adrianwedd.github.io',
  build: {
    inlineStylesheets: 'auto'
  }
});
