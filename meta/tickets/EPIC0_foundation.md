# EPIC 0 — Foundation & Repo Setup

## Ticket 0.1 — Establish Astro project skeleton
**Goal:** Ensure a minimal Astro project exists and runs locally.

**Tasks**
- Create Astro project (or confirm existing).
- Ensure `src/pages`, `src/layouts`, `src/styles`, `public/` exist.
- Confirm Node version and package scripts.

**AC**
- `npm install` succeeds.
- `npm run dev` serves locally.
- `npm run build` produces `dist/` without errors.
- No framework dependencies beyond Astro core.

**Notes / Guardrails**
- Keep it boring. No theme packages.
- Default to no client JS other than the background component.
